# 复制为 config/local.toml 后填写真实值；本文件可入库，local.toml 不入库。
[tushare]
token = "your-tushare-token"      # 注册获取：https://tushare.pro

[llm]
api_key = "your-llm-api-key"      # DeepSeek / Kimi / 任意 OpenAI 兼容服务
# 如需覆盖默认，可再写 provider / base_url / model（默认见 config/default.toml）

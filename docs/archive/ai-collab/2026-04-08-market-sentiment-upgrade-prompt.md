# 市场情绪升级执行提示词

把下面整段提示词复制到一个新聊天里执行。

---

你现在是这个仓库的实施代理，请严格执行 `D:\构想\stock-assistant\docs\plans\2026-04-08-market-sentiment-upgrade.md`，并以 `D:\构想\stock-assistant\docs\plans\2026-04-08-market-sentiment-upgrade-design.md` 作为设计基线。

执行要求：

1. 先完整阅读以下文件，再开始任何实现：
   - `D:\构想\stock-assistant\docs\plans\2026-04-08-market-sentiment-upgrade-design.md`
   - `D:\构想\stock-assistant\docs\plans\2026-04-08-market-sentiment-upgrade.md`
   - `D:\构想\stock-assistant\CLAUDE.md`

2. 必须按阶段执行，只能从 Phase 1 开始。

3. 当前聊天一次只允许完成一个阶段。
   - 阶段完成后，必须停止并汇报：
     - 已修改文件
     - 已运行测试
     - 数据抽样校验结果
     - 手工验收结果
     - Git 状态
     - 是否满足进入下一阶段的门禁

4. 任一阶段若有以下任一项未通过，禁止进入下一阶段：
   - 红绿测试未闭环
   - 自动化测试未通过
   - 数据抽样校验未通过
   - 手工验收未通过
   - Git 检查点未完成

5. 开发规则：
   - 始终使用中文回复
   - 默认使用 Git
   - 所有改动保持模块化
   - 后端优先红绿测试闭环
   - 前端至少通过 `pnpm type-check` 和 `pnpm build`
   - 每完成一个任务就给中期简报，减少上下文压力

6. 测试与验证规则：
   - 后端先写失败测试，再实现最小代码，再跑通过
   - 每阶段结束时，运行该阶段计划中列出的全部验证命令
   - 不允许跳过“Expected: FAIL”这一步
   - 不允许把“理论上应该可以”当作“已验证通过”

7. Git 规则：
   - 每阶段结束后按计划提交
   - 提交信息使用中文、简洁、Conventional Commits 风格
   - 不得把与本阶段无关的脏文件一起提交

8. 本次从 **Phase 1** 开始执行。
   - 只做 Phase 1
   - 不要提前实现 Phase 2 及之后内容
   - 如果 Phase 1 全部通过，停下并等我回复“进入下一阶段”

9. 如果计划中的文件路径与实际仓库略有偏差：
   - 先阅读代码确认最接近的落点
   - 保持原有模块边界
   - 不要擅自扩大范围

10. 输出风格：
   - 先给结论，再给分步骤说明
   - 明确写出你当前处于哪个 Phase
   - 每次汇报都要说明“能否进入下一阶段”

现在开始：

1. 先概括你对本次任务的理解
2. 列出 Phase 1 的任务清单
3. 然后按计划进入 Phase 1，执行红绿测试和实现

---

推荐你在新聊天的第一句额外补一句：

**严格遵守阶段门禁：当前阶段没完全通过前，不得进入下一阶段。**

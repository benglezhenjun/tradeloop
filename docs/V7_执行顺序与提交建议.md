# V7 执行顺序与提交建议

## 推荐执行顺序

### Phase A：准备工作（并行）

- A1: 修复 position.py:178 变量命名
- A2: 创建 codex/v7 分支 + 版本号更新

推荐提交点：
- A1 完成后：`fix: 修复 position.py 变量命名`
- A2 完成后：`chore: V7 版本管理准备`

验证命令：
```bash
cd backend && uv run pytest tests/ -v
```

### Phase B：后端核心功能（顺序执行）

执行顺序：B1 → B2 → B3 → B4

- B1: 数据模型（TradeReview + BehaviorPattern）
- B2: 服务层（review.py + review_contract.py + pattern.py）
- B3: 复盘 LLM Agent（agents/review.py）
- B4: API 路由 + 测试

推荐提交点（B4 完成后统一提交）：
- `feat: 新增V7复盘与行为模式后端能力`

验证命令：
```bash
cd backend && uv run pytest tests/ -v
```

### Phase C：前端功能（顺序执行）

执行顺序：C1 → C2

- C1: 类型定义 + API 封装 + Pinia Store
- C2: 组件（ScoreRadar + PatternCard） + 页面（ReviewView） + 路由导航 + PositionView 集成

推荐提交点（C2 完成后统一提交）：
- `feat: 新增V7复盘前端页面与雷达图`

验证命令：
```bash
cd frontend && pnpm type-check
cd frontend && pnpm build
```

### Phase D：集成验证

- 全量回归测试
- CLAUDE.md 更新
- docs/README.md 更新
- 功能完整性检查清单

推荐提交点：
- `docs: 补齐V7验收与执行文档`

验证命令：
```bash
cd backend && uv run pytest tests/ -v
cd frontend && pnpm type-check
cd frontend && pnpm build
```

## 依赖关系

```
Phase A (并行)
    ├── A1: position.py 修复
    └── A2: 分支 + 版本号
            │
Phase B (顺序: B1 → B2 → B3 → B4)
            │
Phase C (顺序: C1 → C2)
            │
Phase D (集成验证)
```

## 建议提交信息

| 阶段 | 提交信息 |
|------|---------|
| A1 | `fix: 修复 position.py get_position_summary 变量命名` |
| A2 | `chore: V7 版本管理准备` |
| B | `feat: 新增V7复盘与行为模式后端能力` |
| C | `feat: 新增V7复盘前端页面与雷达图` |
| D | `docs: 补齐V7验收与执行文档` |

## 回滚建议

- 后端问题：回滚 Phase B 提交
- 前端问题：回滚 Phase C 提交
- 文档独立回滚，不影响运行逻辑
- Phase A 的修复（变量名）属于低风险改动，无需回滚
